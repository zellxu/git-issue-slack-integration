<% flash.each do |key, value| %>
  <div class="alert <%= bootstrap_class_for(key) %>">
    <%= value %>
    <button class="close" data-dismiss="alert"><%= '×'.html_safe %></button>
  </div>
<% end %>